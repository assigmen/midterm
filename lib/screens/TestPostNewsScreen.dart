import 'package:flutter/material.dart';

class TestPostNewsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Test Post News')),
      body: Center(
        child: Text('Test post news content goes here'),
      ),
    );
  }
}
